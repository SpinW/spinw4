classdef magnetic_structure
    properties
    end

    properties (Constant)
    end

    methods(Access=public)
        % mag_str should be a matlab 'struct' which has 3 fields (F, nExt, k)
        function self = magnetic_structure(mag_str, varargin)
        end
    end

    methods(Access=private)
    end
end
