name: Run SpinW tests

on: pull_request

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Run Matlab
        run: matlab -batch "addpath(genpath('spinw')); run run_tests.m"
      - name: Run Codecov
        run: codecov.exe -f coverage.xml
